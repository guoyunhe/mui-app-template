import { defineConfig } from 'electron-vite';

export default defineConfig({
  main: {
    // vite config options
  },
  preload: {
    // vite config options
  },
  renderer: {
    // vite config options
  },
});
